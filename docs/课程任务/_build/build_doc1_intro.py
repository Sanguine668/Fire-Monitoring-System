"""生成任务1交付物：01_软件介绍（项目简介）.docx"""
from __future__ import annotations

import sys
from pathlib import Path

sys.path.append(str(Path(__file__).resolve().parent))
from common import add_image, add_table, caption, h1, h2, new_document, p, bullets, page_break  # noqa: E402

ROOT = Path(__file__).resolve().parents[3]
OUT = ROOT / "docs" / "课程任务" / "任务1-软件介绍"


def main() -> None:
    OUT.mkdir(parents=True, exist_ok=True)
    doc = new_document(
        "火情智能监测与预警系统",
        "软件介绍（项目简介）",
        [
            ("项目名称", "火情智能监测与预警系统（FireGuard AI）"),
            ("项目类型", "软件项目管理课程小组实训项目"),
            ("项目周期", "2026-09-03 ~ 2026-11-05（8 周，每周一次实训，约 3.5 小时）"),
            ("项目团队", "蔡俊杰（组长）、李汶航、欧阳宇康、陈泽恩"),
            ("文档版本", "v0.1（起草稿）"),
            ("编制日期", "2026 年 9 月 10 日"),
        ],
        doc_number="FGA-INTRO-2026-001",
    )

    h1(doc, "1. 项目概况")
    p(doc, "火情智能监测与预警系统是一套面向园区、仓库、林区等场景的视频火情监测系统。系统接入手机摄像头、"
           "本地上传视频与 RTSP 摄像头等视频源，利用自训练的深度学习模型实时识别画面中的火焰与烟雾，"
           "通过分级预警规则产生告警，并在监控端实时展示检测画面、告警记录与统计数据。")
    p(doc, "系统定位为\"可直接演示、可持续迭代\"的课程实训产品：既包含完整的前后端与检测链路，"
           "也包含数据治理、模型训练与评测等工程过程；在满足课程要求的同时，为后续加入智能体复核与远距离识别预留架构空间。")

    h1(doc, "2. 项目背景与问题")
    p(doc, "火灾早期发现是降低事故损失的关键。传统烟感设备受安装位置与覆盖范围限制，难以覆盖大空间与室外场景；"
           "现有开源烟火检测项目多以实验室近景演示为主，普遍存在三类问题：")
    bullets(doc, [
        "数据质量参差：网络采集图片重复、分辨率低、近景大火居多，远距离小目标样本稀缺；",
        "误报率偏高：灯光、夕阳、蒸汽、云雾等干扰容易被误判为烟火；",
        "工程闭环不完整：多数项目只提供检测脚本，缺少视频源管理、告警闭环、历史统计与演示保障。",
    ])
    p(doc, "本项目针对上述问题，采用\"自训练模型 + 分级预警规则 + 智能体复核\"的技术路线：先用可控数据与规则保证闭环可用，"
           "再通过数据治理、切片检测与视觉智能体复核提升准确率与远距离识别能力。")

    h1(doc, "3. 目标用户与使用场景")
    add_table(
        doc,
        ["用户角色", "主要职责", "使用场景", "获得的价值"],
        [
            ["值班监控人员", "查看实时画面、处理告警", "监控室大屏/浏览器", "第一时间发现烟火并快速核实"],
            ["系统管理员", "管理视频源、参数与模型", "系统设置与视频源管理页", "按现场情况调整阈值与接入设备"],
            ["项目评审/教师", "检查功能与过程文档", "课堂演示与材料评审", "看到完整开发过程与量化结果"],
            ["外部设备（手机/摄像头）", "提供视频流", "局域网/手机热点推流", "无需专有硬件即可接入"],
        ],
        [2.6, 4.2, 3.6, 4.6],
    )

    h1(doc, "4. 系统功能概览")
    add_table(
        doc,
        ["模块", "主要功能", "优先级", "当前状态"],
        [
            ["实时监控", "多路视频墙、实时检测画面与检测框、FPS/延迟/风险分", "高", "已完成（Vue 页面 + MJPEG 链路上线）"],
            ["烟火检测", "YOLO 模型对视频帧进行 fire/smoke 检测", "高", "已完成 fire 单类（自训练 YOLO26n）"],
            ["分级预警", "连续帧 + 阈值 + 冷却时间产生黄/红分级告警", "高", "已完成（状态机 + 落库 + WebSocket 推送）"],
            ["视频源管理", "新增/启停视频源、本地上传视频、手机推流接入", "高", "已完成上传与 file 源，手机源待联调"],
            ["历史告警", "告警列表、确认处理、时间与置信度记录", "高", "已完成接口，前端待接线"],
            ["统计分析", "告警趋势、类型分布、风险统计图表", "中", "页面骨架已完成"],
            ["系统配置", "检测阈值、连续帧、冷却时间等参数配置", "中", "已完成接口与页面骨架"],
            ["智能体复核（规划）", "多尺度检测 + 时序融合 + VLM 复核，降低误报", "高（老师新增要求）", "方案已确定，待开发"],
            ["远距离识别（规划）", "切片推理、小目标训练、远景测试集评估", "高（老师新增要求）", "方案已确定，待开发"],
        ],
        [3.0, 6.4, 2.4, 3.6],
    )

    h1(doc, "5. 技术方案")
    add_table(
        doc,
        ["层次", "技术选型", "说明"],
        [
            ["前端", "Vue3 + Vite + Element Plus + ECharts", "监控总览、实时监控、告警中心、视频源管理、系统设置五个页面"],
            ["后端", "FastAPI + WebSocket + SQLite", "REST 接口、实时事件推送、告警落库与统计"],
            ["视频接入", "OpenCV + MJPEG；IP Webcam（手机）/ 上传 MP4 / RTSP", "统一抽象为视频源，file 源自动循环"],
            ["AI 检测", "Python + Ultralytics YOLO26n（自训练）+ CUDA", "数据集来自 gengyanlei 烟火数据集，RTX 4050 6GB 训练"],
            ["智能体（规划）", "多尺度切片 + 时序融合 + 视觉语言模型复核", "一级本地检测保证实时性，二级复核降低误报"],
            ["部署", "单台 Windows 笔记本，局域网/手机热点", "课堂演示一键启动，支持上传视频保底通道"],
        ],
        [3.0, 5.4, 7.0],
    )

    h1(doc, "6. 技术特色与优势")
    bullets(doc, [
        "手机即插即用：安卓手机通过 IP Webcam 推流，即可作为移动摄像头接入，满足老师对\"手机实时识别\"的要求；",
        "双通道演示：手机实时推流为主，本地上传视频为辅，课堂演示不依赖现场网络与真实火源；",
        "分级预警防误报：不因单帧误检报警，采用连续帧确认、置信度阈值与告警冷却，兼顾灵敏度与稳定性；",
        "自训练模型与量化对比：使用自训练模型，并与 D-Fire、Pyronear、gengyanlei 等公开模型在同一验证集与演示素材上对比；",
        "智能体复核：对高风险或低置信样本做二次视觉判断，区分烟雾、火焰与云雾/灯光/蒸汽等干扰；",
        "远距离识别：切片检测与小目标训练提升远景烟雾召回，并单独建立远景测试集评估；",
        "低成本本地化：单机部署、无需云服务即可完成全部核心功能。",
    ])
    add_table(
        doc,
        ["对比维度", "常见开源/现有方案", "本系统"],
        [
            ["视频源", "仅本地文件或固定摄像头", "手机推流 + 上传视频 + RTSP 统一接入"],
            ["误报控制", "单帧检测即报警", "连续帧 + 阈值 + 冷却 + 智能体复核"],
            ["远距离识别", "以近景大火为主", "切片推理 + 小目标训练 + 远景测试集"],
            ["模型评测", "单一模型演示", "自训练模型与多个公开模型量化对比"],
            ["演示可靠性", "依赖现场条件", "上传视频保底通道 + 全链路预演"],
            ["交付完整度", "仅检测脚本", "前后端系统 + 数据治理 + 项目过程文档"],
        ],
        [3.0, 5.6, 6.8],
    )

    h1(doc, "7. 当前进展与数据现状")
    p(doc, "截至目前，系统已打通\"视频源 → 真实模型检测 → 分级告警 → 历史记录 → 实时画面\"的后端闭环，"
           "前端五个基础页面已完成；端到端冒烟中，上传演示视频后 25 秒内产生 4 条严重明火告警，MJPEG 实时画面正常。")
    p(doc, "同时，我们对训练数据做了全面体检，发现并记录以下问题：数据集中存在 57 组精确重复与 149 组近重复图片；"
           "图像分辨率中位数仅 500 像素宽，67% 的图片宽度小于 640；目标以近景/中景为主，真正的小目标占比只有 14.7%。"
           "这说明现有数据难以支撑远距离识别，且重复图片可能造成验证指标虚高，需要先做数据清洗与防泄漏重划分。", indent=True)
    p(doc, "针对上述结论，项目已制定数据治理与增强计划：清洗重复与低质样本（v1.1）→ 按相似组重划分并重训诚实基线 → "
           "补充烟雾与远距离数据（v2）→ 引入切片检测与智能体复核，最终以量化对比结果说明系统优势。")

    h1(doc, "8. 团队与分工")
    add_table(
        doc,
        ["成员", "角色", "主责交付物"],
        [
            ["蔡俊杰", "组长、技术负责人", "软件介绍、需求规格说明书、功能需求与建模、模型训练与评测"],
            ["李汶航", "前端/文档负责人", "项目章程、非功能与界面需求、进度章节、前端联调与演示脚本"],
            ["欧阳宇康", "需求调查与质量负责人", "需求调查记录、需求追踪矩阵、WBS 字典与 RACI、评审记录"],
            ["陈泽恩", "配置与工具负责人", "模板与格式统一、版本管理、Microsoft Project 建库与甘特图截图"],
        ],
        [2.4, 3.6, 9.4],
    )

    h1(doc, "9. 里程碑与交付物")
    add_table(
        doc,
        ["阶段", "周次", "主要产出"],
        [
            ["启动与需求", "第 1~2 周", "项目章程、软件介绍、需求规格说明书（含需求调查与建模）"],
            ["核心闭环", "第 3~4 周", "真实模型检测、上传视频源、分级告警、实时画面"],
            ["数据与模型", "第 4~6 周", "数据清洗与防泄漏划分、远距离数据集、小目标模型、指标对比"],
            ["智能体增强", "第 5~7 周", "切片推理、时序融合、VLM 复核、误报与远景评估"],
            ["收尾交付", "第 7~8 周", "工作任务分解说明书（含 Project 截图）、系统演示与结项材料"],
        ],
        [3.2, 2.6, 9.6],
    )

    h1(doc, "10. 声明")
    p(doc, "本系统使用的烟火数据集来自开源项目 gengyanlei/fire-smoke-detect-yolov4，作者声明仅限学术探索；"
           "本项目仅用于课程学习与研究，不用于商业用途，相关来源已在参考文档中标注。", indent=True)

    path = OUT / "01_软件介绍（项目简介）.docx"
    doc.save(path)
    print(path)


if __name__ == "__main__":
    main()

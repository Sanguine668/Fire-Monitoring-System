"""生成《图表说明与绘制要点》.docx：把各图逻辑用文字固化，便于手工重绘。"""
from __future__ import annotations

import sys
from pathlib import Path

sys.path.append(str(Path(__file__).resolve().parent))
from common import add_table, h1, h2, new_document, numbers, p  # noqa: E402

ROOT = Path(__file__).resolve().parents[3]
OUT = ROOT / "docs" / "课程任务" / "项目参考材料"


def main() -> None:
    OUT.mkdir(parents=True, exist_ok=True)
    doc = new_document(
        "火情智能监测与预警系统",
        "图表说明与绘制要点",
        [
            ("用途", "把各图的组成与逻辑用文字固化，便于手工重绘、评审与引用"),
            ("适用文档", "软件介绍、需求规格说明书、工作任务分解说明书"),
            ("版本 / 日期", "v1.0 / 2026 年 9 月 10 日"),
            ("编制", "蔡俊杰"),
        ],
        doc_number="FGA-FIG-2026-001",
    )

    h1(doc, "1. 图表清单")
    add_table(
        doc,
        ["图号", "名称", "用途", "建议出现位置", "建议工具"],
        [
            ["图 1", "系统总体架构", "说明系统分层与各层职责", "软件介绍、需求规格说明书", "Visio / ProcessOn / draw.io"],
            ["图 2", "系统数据流", "说明一帧视频从输入到告警展示的完整链路", "需求规格说明书、设计文档", "Visio / ProcessOn"],
            ["图 3", "告警判定状态机", "说明预警级别如何产生、复位与冷却", "需求规格说明书、设计文档", "Visio / draw.io"],
            ["图 4", "WBS 树状图", "展示工作分解层级与编号", "工作任务分解说明书", "ProcessOn / Microsoft Project"],
            ["图 5", "项目甘特图", "展示任务工期与里程碑", "工作任务分解说明书", "Microsoft Project"],
        ],
        [1.6, 3.0, 4.6, 4.2, 2.0],
    )

    h1(doc, "2. 图 1 系统总体架构：绘制逻辑")
    p(doc, "绘制逻辑：自上而下分成六层，每层用一个横向长条表示；层与层之间只用单向箭头连接，表示依赖与数据方向，"
           "不要画交叉斜线。层内用短词列举职责，每层不超过 6 个关键词。", indent=True)
    add_table(
        doc,
        ["层次（自上而下）", "层内内容", "与上下层的连接"],
        [
            ["用户层", "值班人员、系统管理员、项目评审/教师", "向下：操作与查看请求"],
            ["展示层", "Vue3 + Element Plus + ECharts；监控总览、实时监控、告警中心、视频源管理、系统设置", "向下：REST 接口、WebSocket 事件、MJPEG 画面流"],
            ["服务层", "FastAPI；REST 接口、WebSocket 推送、MJPEG 流、检测 worker、告警规则引擎", "向下：调用检测能力；向右：读写数据存储"],
            ["AI 与智能体层", "左：智能体复核（多尺度切片 + 时序融合 + 视觉语言模型复核，规划中）；右：YOLO26n 自训练检测模型", "向下：从视频接入层取帧"],
            ["视频接入层", "手机 IP Webcam 推流、本地上传 MP4（循环）、RTSP 摄像头", "向下：视频来源"],
            ["数据存储与运行环境", "SQLite（视频源/告警/配置）+ 上传文件目录；Windows 笔记本（RTX 4050）单机部署", "作为底座，不用箭头"],
        ],
        [3.2, 8.0, 4.2],
    )
    p(doc, "可直接写入文档的文字描述：系统自上而下分为用户层、展示层、服务层、AI 与智能体层、视频接入层，"
           "底层为数据存储与运行环境。用户通过浏览器访问展示层；展示层通过 REST 接口、WebSocket 事件与 MJPEG 画面流与服务层交互；"
           "服务层中的检测 worker 调用 AI 层模型，对视频接入层的画面进行抽帧检测；检测结果由告警规则引擎判定后写入数据存储，"
           "并实时推送到前端展示。智能体复核作为 AI 层的一个分支，对低置信或高风险样本做二次判断。")
    h2(doc, "重绘要点")
    numbers(doc, [
        "先画 6 个等宽横向矩形，间距一致，边距留白；",
        "箭头只连接相邻层，竖直向下；服务层到数据存储可用一条向右箭头；",
        "颜色：功能层浅蓝、AI 层浅橙、智能体层浅红、存储层浅绿、用户层与运行环境浅灰；",
        "文字用 10~12 磅，层内项目用顿号分隔，不要写整句；",
        "导出 PNG（300dpi）后插入文档，图题写在图下方。",
    ])

    h1(doc, "3. 图 2 系统数据流：绘制逻辑")
    add_table(
        doc,
        ["步骤", "环节", "输入", "处理", "输出"],
        [
            ["1", "视频源接入", "手机推流 / 上传视频 / RTSP", "统一为视频源对象并按需启动检测任务", "视频帧序列"],
            ["2", "抽帧", "视频帧序列", "按 3~5 FPS 取帧", "待检测图像帧"],
            ["3", "模型检测", "图像帧", "YOLO26n 推理，输出火/烟目标框与置信度", "检测结果列表"],
            ["4", "告警判定", "检测结果序列", "连续帧确认 + 阈值 + 冷却时间（智能体复核为规划增强）", "状态、风险分、告警事件"],
            ["5", "存储与推送", "告警事件、标注帧", "写入 SQLite，WebSocket 广播，缓存最新标注帧", "告警记录、实时事件、MJPEG 帧"],
            ["6", "前端展示", "事件与画面流", "更新视频墙、检测框、告警弹窗与统计", "用户可见的监控界面"],
        ],
        [1.2, 2.4, 4.0, 5.2, 2.6],
    )
    p(doc, "支路说明：标注后的帧单独缓存，供 MJPEG 接口输出到前端；告警记录除实时推送外，还用于历史查询与统计图表。"
           "绘制时主线用一条横向流程，支路用虚线箭头指向右侧的\"历史与统计\"方框。")

    h1(doc, "4. 图 3 告警判定状态机：绘制逻辑（重点）")
    p(doc, "这张图必须与代码实现一致：明火不经过黄色预警，直接进入红色告警；烟雾必须先做连续帧计数。", indent=True)
    add_table(
        doc,
        ["状态", "含义", "进入条件"],
        [
            ["normal", "正常状态，无有效烟火目标", "当前帧没有超过阈值的 fire/smoke，或从其他状态复位"],
            ["疑似烟雾（计数中）", "检出烟雾但未达到连续帧要求", "当前帧 smoke 置信度 ≥ 阈值，计数 +1"],
            ["warning（黄色预警）", "连续检出烟雾达到 N 帧", "smoke 计数 ≥ 连续帧数（默认 3）"],
            ["critical（红色告警）", "检出明火，需立即处置", "当前帧 fire 置信度 ≥ 阈值（任意状态都可进入）"],
        ],
        [3.0, 5.6, 6.8],
    )
    add_table(
        doc,
        ["序号", "迁移", "条件", "动作"],
        [
            ["1", "normal → 疑似烟雾", "smoke ≥ 阈值", "计数置 1，状态不变为告警"],
            ["2", "疑似烟雾 → 疑似烟雾", "连续帧仍检出 smoke", "计数 +1"],
            ["3", "疑似烟雾 → warning", "计数 ≥ 连续帧数 N", "状态置 warning，风险分 45 + 25×置信度；冷却允许则落库黄色告警"],
            ["4", "任意状态 → critical", "fire ≥ 阈值", "状态置 critical，风险分 80 + 15×置信度，烟雾计数清零；冷却允许则落库红色告警"],
            ["5", "疑似烟雾 / warning → normal", "当前帧无有效烟火目标", "计数清零，状态置 normal，风险分置 0"],
            ["6", "critical → normal", "后续帧不再检出有效 fire", "同序号 5"],
            ["7", "任意状态 → normal（复位）", "视频源断流或重连", "计数、冷却计时、风险分全部复位，避免重连误报"],
        ],
        [1.4, 3.6, 3.8, 6.6],
    )
    p(doc, "冷却规则：距上次告警落库不足 8 秒时，只更新状态与风险分，不重复写入告警记录。"
           "绘制时不要把冷却画成一个状态，应写成图注或旁注文字。")
    h2(doc, "重绘要点")
    numbers(doc, [
        "四个状态排成\"上三下一\"：normal、疑似烟雾、warning 在上排，critical 在下排居中；",
        "烟雾链路用橙色实线箭头（normal→疑似→warning），并在疑似烟雾上加一个自环箭头表示计数 +1；",
        "明火链路用红色箭头，从 normal、疑似烟雾、warning 各画一条指向 critical，旁注\"任意状态检出 fire 即触发\"；",
        "复位链路用绿色虚线箭头回到 normal，旁注\"当前帧无有效目标，计数清零\"；",
        "冷却与离线两条规则写在图下方的方框内，不占用状态节点。",
    ])

    h1(doc, "5. 图 4 WBS 树状图：绘制逻辑")
    p(doc, "分解形式采用\"项目阶段 + 交付物\"混合分解，共三层，编号规则为 1.0 / 1.1 / 1.1.1。"
           "同一层级横向排列，子节点挂在其父节点正下方，避免连线交叉。", indent=True)
    add_table(
        doc,
        ["编号", "层级内容", "包含的工作包示例"],
        [
            ["1.0", "火情智能监测与预警系统项目", "整个项目"],
            ["1.1", "项目管理", "1.1.1 项目启动与章程；1.1.2 计划与进度控制；1.1.3 会议与文档管理"],
            ["1.2", "需求与设计", "1.2.1 需求调查；1.2.2 需求规格说明书；1.2.3 架构与接口设计"],
            ["1.3", "系统开发", "1.3.1 前端页面；1.3.2 后端接口；1.3.3 视频接入与实时画面"],
            ["1.4", "数据与模型", "1.4.1 数据清洗；1.4.2 烟类与远距离数据补充；1.4.3 模型训练与评测"],
            ["1.5", "测试与评估", "1.5.1 功能测试；1.5.2 误报与远距离评估；1.5.3 缺陷修复验证"],
            ["1.6", "交付与结项", "1.6.1 演示脚本与彩排；1.6.2 结项报告；1.6.3 材料归档"],
        ],
        [1.8, 4.6, 9.0],
    )
    p(doc, "可直接写入文档的文字描述：项目按六个阶段分解为 1.1~1.6 六个二级节点，每个二级节点下再分解出三级工作包，"
           "工作包编号在其父节点编号后追加序号；每个工作包只设一名责任人，验收标准与交付物在 WBS 字典中说明。")

    h1(doc, "6. 图 5 项目甘特图：绘制逻辑")
    numbers(doc, [
        "横轴为周次（第 1~8 周），纵轴为 WBS 三级工作包；",
        "用条形表示工期，条形颜色区分阶段；里程碑用菱形标记（如需求定稿、首轮模型完成、系统联调、结项演示）；",
        "关键路径任务用红色边框标出，前置关系用箭头连接；",
        "图注写明：数据来源为 Microsoft Project 导出的进度计划，导出日期随文档版本更新。",
    ])

    h1(doc, "7. 图题与正文引用规范")
    numbers(doc, [
        "图题写在图的正下方，格式为\"图 N 名称\"，如\"图 3 告警判定状态机\"；",
        "表题写在表格上方，格式为\"表 N 名称\"；",
        "正文引用使用\"如图 N 所示\"\"见表 N\"，每张图在正文中至少被引用一次；",
        "同一份文档内编号连续，不跨文档复用编号。",
    ])

    path = OUT / "图表说明与绘制要点.docx"
    doc.save(path)
    print(path)


if __name__ == "__main__":
    main()
